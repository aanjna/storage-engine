# storage-engine

The
storage engine which is responsible for the maintenance of persistent data within the database
management system.

## Prerequisites:
Java jdk 21

### Cloning the Repository 


## How to run

### Building the Project with Maven:
```
 $ mvn clean install 
```

Run the Spring Boot app 
```
(e.g., mvn spring-boot:run)
```

Connect via telnet localhost 8080